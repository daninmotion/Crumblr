<%= image_tag post.image.url(:medium) %>

<p>
  <h2><%= post.title %></h2>
</p>

<p>
  <%= post.content.truncate(100).html_safe %>
</p>

<p class="text-muted">
	Posted on <%= post.created_at.to_formatted_s(:long) %>
</p>